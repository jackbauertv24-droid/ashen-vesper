# Production assets

No validated engine assets yet. The first environment concept lives in `../concepts/`.

Add assets here after separation, cleanup, and the relevant checks in the asset specification.
