# Brazier alpha cutout

Exactly one built-in image_gen edit call, using the retained hanging-brazier-v001.png as reference. Exact prompt: PROMPT.txt. No retry or API fallback. Actual model identifier, token count, quota debit and money cost were not exposed and remain unknown.

The output is preserved unchanged: 1254 × 1254, RGBA color type 6. It contains 1,228,338 fully transparent pixels and 343,071 partially transparent pixels. Exterior and sampled chain gaps are alpha zero. It missed the requested 1024 canvas size; runtime scales the full image to 84 × 100 as before. No dark key, screen blend or background-removal mask is used for this prop.

Integrated in the encounter, retaining all prior sources. Antialiased metal edges and flame details remain partially transparent; native alpha is preferable to the former backdrop key, but visual review over white/black/abbey is still required. Hash and dimensions are recorded in the catalog, and source/prompt/notes are preservation-locked.
