# Hollow Pilgrim intermediate swing candidate

One authorized built-in `image_gen` call; no correction call or API fallback. The exact prompt is in PROMPT.txt. Reference: the retained `art/production/enemies/hollow-pilgrim-motion-v001.png`. Model identifier, token usage, quota debit and monetary cost were not exposed and remain unknown.

The unchanged output supplies six preparation, raised, descending, contact and recovery studies. It fails the requested transparent background and exact cell grid: a shaded studio backdrop is baked in, feet do not share the requested ground level, and the raised weapon crowds the sheet edge. It is **candidate reference art, not a production export or runtime replacement**.

Keep all originals. Future contributors can use its intermediate body poses to complete the existing sparse swing, but must produce consistent ground anchors, complete weapons, clean alpha and runtime contact evidence. No automated validation claim is made for a nonexistent export.

SHA-256 and dimensions are registered in art/manifest.json. The source, prompt and these notes are locked in art/library/submissions.lock.json. This attempt is preserved even though it is not used by the demo.
